---
- hosts: win
#  become: yes
#  become_user: Administrator
  gather_facts: no

  tasks:
  - name: test service
    win_service:
      name: msiserver
      state: started
    register: service_status

  - debug:
      var: service_status
